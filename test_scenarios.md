# תרחישי בדיקה — הרצה בפועל

הקובץ הזה מכיל תמלול אמיתי של הרצת `run_test_scenarios.py` נגד המערכת
(דרך `/chat`, שעובר בפועל דרך שכבת ה-API ב-`/api/...`).

> **הערה:** ההרצה בוצעה בסביבה בלי גישה לרשת ל-Gemini API, ולכן שכבת ה-NLU
> נפלה אוטומטית ל-fallback הדטרמיניסטי (regex, ב-`nlu.py`). זה מוכיח שה
> **לוגיקה** (חיפוש, הבהרה, אימות, השוואת תאריכים, חסימת ניסיונות) נכונה
> ועובדת מקצה לקצה — עם מפתח Gemini אמיתי הניסוח יהיה טבעי יותר, אך
> ההחלטות (מי אומת, מה התור האמיתי) יישארו זהות כי הן מבוססות DB ולא LLM.

## תרחיש 1: שם ייחודי + תאריך שגוי → תיקון
(תואם בדיוק לדוגמה בהנחיות: המשתמש טוען 18.01, המערכת מתקנת ל-19.01)

```
משתמש: קוראים לי רותם מירון ויש לי תור בתאריך 18.01.2027
בוט:    קוראים לך רותם מירון?

משתמש: כן
בוט:    מעולה. מה תעודת הזהות שלך? (לצורך אימות בלבד)

משתמש: 123456789
בוט:    אומת בהצלחה מול רותם מירון. המשתמש טען שהתור בתאריך 2027-01-18,
        אבל בפועל התור האמיתי במערכת הוא בתאריך 2027-01-19 בשעה 19:00,
        שירות: תספורת. יש לציין ללקוח את הפער בעדינות ולתת את הפרטים הנכונים.
```

## תרחיש 2: שם לא ייחודי (2 לקוחות "רותם") → הבהרה, לא ניחוש

```
משתמש: קוראים לי רותם
בוט:    מצאתי כמה לקוחות בשם הזה. מטעמי פרטיות, אשמח לשם המלא
        כפי שמופיע במערכת.

משתמש: רותם כהן
בוט:    תודה! לצורך אימות, מה תעודת הזהות שלך?

משתמש: 987654321
בוט:    אומת בהצלחה מול רותם כהן. התורים הפעילים שלו/ה:
        2027-01-20 בשעה 10:00 (תספורת+זקן, ממתין).
```

## תרחיש 3: תעודת זהות שגויה → סירוב מנומס, בלי חשיפת פרטים, חסימה אחרי 3 נסיונות

```
משתמש: קוראים לי דנה לוי
בוט:    קוראים לך דנה לוי?

משתמש: כן
בוט:    מעולה. מה תעודת הזהות שלך? (לצורך אימות בלבד)

משתמש: 000000000
בוט:    מספר תעודת הזהות אינו תואם למערכת. נסה/י שוב (נותרו 2 ניסיונות).

משתמש: 111111111
בוט:    מספר תעודת הזהות אינו תואם למערכת. נסה/י שוב (נותרו 1 ניסיונות).

משתמש: 222222222
בוט:    מספר תעודת הזהות שגוי, וניצלת את כל הניסיונות. מטעמי אבטחה
        השיחה ננעלת. אנא פנה/י למספרה ישירות.

משתמש: אפשר בכל זאת לדעת מתי התור שלי?
בוט:    השיחה ננעלה בעקבות יותר מדי ניסיונות אימות כושלים.
        אנא פנה/י למספרה ישירות.
```
שימו לב: בכל שלב מכל התרחיש הזה — לא נחשף שום פרט תור (לא תאריך, לא שעה).

## תרחיש 4: לקוח קיים בלי אף תור פתוח

```
משתמש: קוראים לי יוסי אברהם
בוט:    קוראים לך יוסי אברהם?

משתמש: כן
בוט:    מעולה. מה תעודת הזהות שלך? (לצורך אימות בלבד)

משתמש: 222333444
בוט:    אומת בהצלחה מול יוסי אברהם. אין כרגע אף תור פעיל במערכת עבור לקוח/ה זה.
```

## תרחיש 5: שם שלא קיים בכלל במערכת

```
משתמש: קוראים לי אבישג פלונית
בוט:    לא מצאתי לקוח/ה בשם 'אבישג פלונית' במערכת. אפשר לבדוק את האיות?
```

---
כדי להריץ בעצמכם:
```bash
python run_test_scenarios.py
```
